## 一、概念

**词语义表征**指的是把自然语言中的**词语或短语转换为计算机可以理解的形式**，即将词语映射到一个向量空间或结构化表示中，以便于计算机进行语义分析、文本理解或下游任务（如文本分类、机器翻译、问答等）。

* 目标：捕捉**词义、语法特征、上下文信息、语义关系**。
* 核心：**相似词语在表示空间中应该更接近**。

---

## 二、方法分类

词语语义表征方法大致可以分为三类：

### 1. 传统统计方法（基于计数）

* **基本思想**：通过词的共现频率或上下文统计来表示词义。
* **典型方法**：

  1. **One-hot Encoding**

     * 特点：每个词用向量表示，长度等于词表大小，只有该词位置为1，其余为0。
     * 问题：维度高、稀疏、不表达词义相似性。
  2. **词-文档矩阵 / 词-上下文矩阵（Count-based）**

     * 统计词在文档或上下文中出现的频率（如TF-IDF）。
     * 可以计算词与词之间的**相似度（如余弦相似度）**。
  3. **LSA / LSI（潜在语义分析/索引）**

     * 对词-文档矩阵做SVD降维，捕捉潜在语义结构。
     * 优点：降低维度，得到稠密向量；能捕捉同义关系。
* **优点**：理论简单，可解释性强。
* **缺点**：无法处理上下文多义词，向量静态。

---

### 2. 预测模型（基于神经网络/嵌入）

* **基本思想**：利用神经网络从大规模语料中学习词向量，使语义相似的词向量在空间中距离接近。
* **代表方法**：

  1. **Word2Vec（Mikolov et al., 2013）**

     * **CBOW**：预测中心词的上下文。
     * **Skip-gram**：预测上下文的中心词。
     * 特点：向量稠密、低维、能够捕捉语义和部分语法关系。
  2. **GloVe（Pennington et al., 2014）**

     * 基于全局共现矩阵的词向量训练方法。
     * 优点：融合了全局统计信息和局部上下文。
  3. **FastText**

     * 将词分解为子词（n-gram）向量，有助于处理形态丰富语言和未登录词。
* **特点**：

  * 向量稠密（一般几十到几百维）
  * 能捕捉语义相似度（如“king - man + woman ≈ queen”）
  * 静态表示：一个词只有一个向量，无法表示上下文多义。

---

### 3. 上下文相关表征（Contextualized Embedding）

* **基本思想**：同一个词在不同上下文中表示不同向量，解决多义词问题。
* **代表方法**：

  1. **ELMo（2018）**

     * 使用双向LSTM构建上下文词向量。
     * 根据上下文动态生成词向量。
  2. **BERT / RoBERTa / GPT 系列**

     * 基于Transformer，预训练语言模型。
     * 每个词的向量会根据句子上下文动态变化。
     * 可以直接用于下游任务（分类、问答、NER等）。
* **特点**：

  * 词向量动态，捕捉上下文语义。
  * 更高维（通常768维或更多）。
  * 可迁移性强。

---

## 三、词语义表征的特性

1. **稠密性 vs 稀疏性**

   * 传统计数方法通常稀疏，高维。
   * 神经嵌入方法通常稠密，低维。

2. **静态 vs 动态**

   * Word2Vec、GloVe → 静态词向量
   * BERT、ELMo → 上下文动态词向量

3. **可解释性**

   * 传统方法可解释性强。
   * 神经网络嵌入难以直接解释向量含义，但可以通过相似度、投影可部分理解。

4. **语义能力**

   * 传统方法捕捉共现语义。
   * 神经嵌入捕捉更丰富语义，包括类比关系。
   * 上下文向量可以处理多义词和复杂语义依赖。

---

## 四、词语义表征的应用

1. **文本分类**（情感分析、垃圾邮件检测）
2. **信息检索 / 搜索引擎**（相似度匹配）
3. **机器翻译**（表示源语言和目标语言词义）
4. **问答系统 / 对话系统**
5. **知识图谱 / 关系抽取**
6. **推荐系统**（基于文本特征的用户兴趣表示）

---

## 五、示意图

```
           词语
             │
  ┌──────────┴──────────┐
  │                     │
统计方法               神经网络嵌入
  │                     │
One-hot, TF-IDF      Word2Vec, GloVe, FastText
  │                     │
LSA/LSI               上下文嵌入
                        │
                     ELMo, BERT, GPT
```

---