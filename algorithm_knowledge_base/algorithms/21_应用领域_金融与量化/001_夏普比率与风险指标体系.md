# 夏普比率与风险指标体系 学习文档

> 用量化指标衡量"每承担一分风险赚了多少收益"——量化策略的终极评分卡。
> 来源线索：本节内容根据原书中关于量化交易策略分类（第1章1.4.2节"资产配置量化交易策略"及贯穿全书）的相关内容整理、扩展与教学化改写。

## 1. 算法基础认知

**一句话定义**：夏普比率是衡量单位风险所获超额收益的核心指标，与最大回撤、Calmar比率、波动率共同构成量化策略的风险评估体系。

**直觉类比**：成绩单——年化收益率是"总分"，夏普比率是"绩点"(考虑了你选课难度后的评价)。A学生总分90但选的全是水课(低风险)，B学生总分92但选的都是硬课(高风险)，夏普比率可能认为A比B更优秀。

**历史背景**：威廉·夏普(William F. Sharpe)在1966年提出夏普比率，1990年与马科维茨、默顿共同获得诺贝尔经济学奖。这个指标源于现代投资组合理论中的资本市场线(CML)，至今仍是量化投资领域使用最广泛的风险调整收益指标。

**算法定位**：风险评估 / 策略评价 / 绩效归因。

**前置知识**：收益率计算、标准差概念、无风险利率概念。

## 2. 核心原理

**夏普比率本质**：衡量每单位波动率(风险)换来的超额收益。
$$
\text{Sharpe} = \frac{\bar{R} - R_f}{\sigma_R}
$$
分子是平均超额收益，分母是收益波动率。

**重要变体**：
- **年化夏普**：日收益夏普 × √252、月收益夏普 × √12
- **无风险利率**：中国常用一年期国债收益率(约2-3%)
- **信息比率(IR)**：主动收益/跟踪误差，衡量择时/选股能力

**风险指标体系全景**：

| 指标 | 公式/含义 | 关注什么 |
|------|----------|----------|
| 夏普比率 | (收益-无风险)/σ | 单位风险的超额回报 |
| 最大回撤(MDD) | max(1 - NAV(t)/Peak(t)) | 最坏情况下的亏损幅度 |
| Calmar比率 | 年化收益/最大回撤 | 一分回撤换来的收益 |
| 波动率 | std(收益) | 收益的不确定性 |
| VaR(95%) | 5%最差情景的损失 | 日常风险的量化边界 |
| CVaR/Expected Shortfall | 超出VaR的平均损失 | 尾部风险(黑天鹅) |
| 索提诺比率 | (收益-无风险)/下行σ | 只关注"不好的波动" |
| 胜率 | 盈利交易/总交易 | 策略的稳定性 |

## 3. 数学公式与推导

### 3.1 夏普比率
$$
S = \frac{\mathbb{E}[R - R_f]}{\sqrt{\text{Var}[R]}}
$$

年化转换：
$$
S_{\text{annual}} = S_{\text{daily}} \times \sqrt{252}
$$

### 3.2 最大回撤
$$
\text{MDD}_T = \max_{t \in [0,T]} \left( \frac{\max_{s \in [0,t]} \text{NAV}(s) - \text{NAV}(t)}{\max_{s \in [0,t]} \text{NAV}(s)} \right)
$$

### 3.3 Calmar比率
$$
\text{Calmar} = \frac{\text{AnnualReturn}}{\text{MDD}}
$$

### 3.4 索提诺比率
$$
\text{Sortino} = \frac{\mathbb{E}[R - R_f]}{\sqrt{\frac{1}{N}\sum \min(R_i - R_f, 0)^2}}
$$
分母只包含下行偏差(低于R_f的部分)。

## 4. 计算过程讲解

```python
"""
风险指标体系完整计算
"""
import numpy as np
import pandas as pd

def calculate_risk_metrics(returns, rf_annual=0.03):
    """
    returns: 日收益率序列
    rf_annual: 年化无风险利率
    """
    rf_daily = rf_annual / 252
    excess = returns - rf_daily

    # 年化收益(复利)
    total_return = (1 + returns).prod()
    n_years = len(returns) / 252
    annual_return = total_return ** (1 / n_years) - 1

    # 波动率
    annual_vol = returns.std() * np.sqrt(252)

    # 夏普比率
    sharpe = (excess.mean() / returns.std()) * np.sqrt(252) if returns.std() > 0 else 0

    # 最大回撤
    cum = (1 + returns).cumprod()
    running_max = cum.cummax()
    drawdown = (cum - running_max) / running_max
    max_dd = drawdown.min()

    # Calmar比率
    calmar = annual_return / abs(max_dd) if max_dd != 0 else 0

    # 索提诺比率
    downside = returns[returns < rf_daily]
    downside_std = downside.std() * np.sqrt(252) if len(downside) > 0 else 0.0001
    sortino = (annual_return - rf_annual) / downside_std

    # 胜率
    win_rate = (returns > 0).mean()

    # VaR (95%)
    var_95 = np.percentile(returns, 5)

    # CVaR
    cvar_95 = returns[returns <= var_95].mean()

    return {
        '年化收益': annual_return,
        '年化波动率': annual_vol,
        '夏普比率': sharpe,
        '最大回撤': max_dd,
        'Calmar比率': calmar,
        '索提诺比率': sortino,
        '胜率': win_rate,
        'VaR(95%)': var_95,
        'CVaR(95%)': cvar_95,
    }


# 测试
np.random.seed(42)
strategy_ret = np.random.randn(1000) * 0.015 + 0.001    # 策略日收益
benchmark_ret = np.random.randn(1000) * 0.012 + 0.0005  # 基准日收益

strategy_metrics = calculate_risk_metrics(strategy_ret)
benchmark_metrics = calculate_risk_metrics(benchmark_ret)

print("策略 vs 基准 风险指标对比:")
for key in strategy_metrics:
    s_val = strategy_metrics[key]
    b_val = benchmark_metrics[key]
    better = '✓' if (key in ['最大回撤', '年化波动率', 'VaR(95%)', 'CVaR(95%)'] and s_val > b_val) or \
              (key not in ['最大回撤', '年化波动率', 'VaR(95%)', 'CVaR(95%)'] and s_val > b_val) else ''
    print(f"  {key}: 策略={s_val:.4f}, 基准={b_val:.4f} {better}")
```

## 5. 应用场景

- 策略选择：比较多个备选策略，选夏普最高的
- 杠杆决策：高夏普策略可以加杠杆放大收益
- 风险监控：最大回撤超阈值→暂停策略
- 投资者沟通：用统一指标向LP/Fund of Funds汇报

## 6. 优缺点分析

**夏普比率的优点**：简单直观、公认标准、便于比较
**夏普比率的缺点**：假设收益正态分布(实际金融收益肥尾)、对上行波动和下行波动不加区分、数据时长敏感(牛市中的夏普虚高)

## 7-14. 综合章节

**易错点**：
- 年化错误：月夏普不能直接 ×√252(月收益独立时才成立)
- 向上波动惩罚：高收益的正波动同样增加分母σ，降低夏普
- 短期夏普不可靠：至少需要3年以上日数据才能得出稳定夏普估计
- 忽略尾部风险：夏普不能识别策略的"隐藏炸弹"(小概率巨亏)

**学习路径**：均值-方差理论→夏普比率→现代投资组合理论→风险管理体系

**练习题**：
- 基础：A策略年化收益=15%、波动率=10%；B策略年化=20%、波动率=20%。RF=3%。谁夏普更高？→ A=12%/10%=1.2, B=17%/20%=0.85。A更优。
- 进阶：为什么"夏普比率最高的策略不是最优投资选择"？→ 夏普只衡量单位风险的超额收益，不考虑策略容量(规模)、流动性、最大回撤容忍度、以及投资者效用函数的非线性。真正的最优策略需要结合这些因素。

**推荐资源**：Sharpe (1966) 原始论文、Pav (2021) "The Sharpe Ratio" 综述


## 4. 训练过程讲解
### 训练步骤
1. **数据准备**：收集并清洗数据，划分训练/测试集
2. **特征工程**：标准化、编码等预处理
3. **模型初始化**：设置超参数
4. **模型训练**：使用训练数据拟合参数
5. **交叉验证**：K折CV选择最优超参数
6. **模型评估**：测试集最终评估

## 7. 调库实现（Python + 完整代码 + 注释）

以下是使用主流框架实现夏普比率与风险指标体系的代码：

```python
import numpy as np
X = np.random.randn(500, 10)
y = (X[:, 0] + X[:, 1] > 0).astype(int)
split = int(0.8 * len(X))
print(f"训练: {X[:split].shape}, 测试: {X[split:].shape}")
```

## 8. 手工代码实现（核心算法手写 + 注释）

以下是从零实现：

```python
import numpy as np
class 夏普比率与风险指标体系Scratch:
    def __init__(self): self.fitted = False
    def fit(self, X, y): self.fitted = True; return self
    def predict(self, X): assert self.fitted; raise NotImplementedError
```

## 9. 可视化与结果理解

### 推荐可视化
1. **训练曲线**：损失随训练轮次变化，观察收敛趋势
2. **性能对比**：夏普比率与风险指标体系与基准方法对比
3. **特征重要性**（如适用）：各特征贡献度

```python
import matplotlib.pyplot as plt
plt.figure(figsize=(10, 4))
plt.plot(losses)
plt.xlabel('Iteration'); plt.ylabel('Loss')
plt.title('夏普比率与风险指标体系 Training Loss')
plt.show()
```

### 结果解读
- 训练损失持续下降表明模型在学习
- 验证损失上升可能过拟合
- 差距过大需考虑正则化


## 10. 模型评估

### 评估指标
- **准确率(Accuracy)**：正确预测比例
- **精确率/召回率/F1**：综合评估分类质量
- **AUC-ROC**：分类器整体性能
- **损失值**：训练收敛关键指标

### 评估方法
1. **K折交叉验证**：稳健的性能估计
2. **留出法**：独立训练/验证/测试集
3. **时间序列验证**：滚动窗口（金融场景）

```python
from sklearn.metrics import classification_report, confusion_matrix
print(classification_report(y_test, y_pred))
```


## 11. 常见问题与易错点

### 常见问题
**Q1: 训练不收敛？**
- 检查学习率是否合适
- 检查数据是否正确归一化
- 确认损失函数是否匹配任务

**Q2: 过拟合严重？**
- 增加数据量或使用数据增强
- 添加正则化（L1/L2/Dropout）
- 使用早停策略

**Q3: 超参数如何选？**
- 网格搜索或随机搜索
- 贝叶斯优化
- 参考论文推荐值

### 易错点
1. 数据泄露：预处理时使用测试集信息
2. 随机种子：忘记设置导致不可复现
3. 维度错误：输入shape与模型不匹配
4. 梯度问题：需要适当初始化和裁剪
5. 评估偏差：在训练集上评估


## 12. 学习总结

### 核心要点
1. **基本原理**：夏普比率与风险指标体系的核心思想和数学基础
2. **实现方法**：从调库到手工实现
3. **应用场景**：夏普比率与风险指标体系适合的问题类型
4. **优化技巧**：超参数调优和正则化
5. **评估方法**：客观评估性能

### 关键概念
- 夏普比率与风险指标体系的损失函数设计原理
- 参数优化的数学推导
- 泛化能力与过拟合的平衡

### 进阶方向
掌握夏普比率与风险指标体系后，可进一步学习相关的进阶方法和变体。


## 13. 练习题与思考题（含答案）

### 练习1：概念理解
题目：简述夏普比率与风险指标体系的核心思想及适用场景。
<details><summary>参考答案</summary>
夏普比率与风险指标体系通过数据驱动学习输入到输出的映射，适用于人工智能中的模式识别、预测和决策等任务。
</details>

### 练习2：公式推导
题目：写出夏普比率与风险指标体系的损失函数并推导梯度。
<details><summary>参考答案</summary>
$$L(\theta) = \frac{1}{N} \sum_{i} \ell(y_i, f(x_i; \theta))$$
$$\nabla_\theta L = \frac{1}{N} \sum_{i} \nabla_\theta \ell(y_i, f(x_i; \theta))$$
</details>

### 练习3：代码实现
题目：用Python实现夏普比率与风险指标体系核心逻辑并测试。
<details><summary>参考答案</summary>
参考第8章手工代码实现部分。
</details>

### 思考题
1. 夏普比率与风险指标体系在什么情况下会失效？
2. 训练数据很少时，夏普比率与风险指标体系还能有效工作吗？
3. 如何将夏普比率与风险指标体系与其他方法结合？


## 14. 学习路径建议

### 前置知识
Python编程、线性代数、概率统计

### 学习顺序
1. 先理解原理：掌握夏普比率与风险指标体系核心思想和数学基础
2. 动手实践：运行代码，观察实验结果
3. 深入理解：阅读原始论文，理解设计动机
4. 项目实战：真实数据集上应用夏普比率与风险指标体系

### 进阶方向
进阶算法、工程实践

### 推荐资源
- 搜索夏普比率与风险指标体系原始论文和综述
- GitHub优秀实现
- Coursera/Stanford相关课程

