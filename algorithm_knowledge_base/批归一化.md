# 批归一化 学习文档

## 1. 算法基础认知
加速训练并提高模型稳定性的技术，对每一层的输入进行归一化。

## 2. 核心原理
对输入进行零均值、单位方差归一化，再进行缩放和平移。

## 3. 数学公式
$y = \gamma rac{x-\mu}{\sqrt{\sigma^2+\epsilon}} + eta$

## 4. 训练过程
- 训练时：计算batch的均值和方差
- 测试时：使用移动平均的均值和方差

## 5. 应用场景
- 深度学习中广泛使用
- 加速收敛

## 6. 优缺点
优点：减少内部协变量偏移；缺点：增加计算量

## 7. 代码实现
```python
import torch.nn as nn
bn = nn.BatchNorm1d(num_features)
```

## 8. 手工实现
```python
import numpy as np

def batch_norm_forward(x, gamma, beta, eps=1e-5):
    # x shape: (N, D)
    mu = np.mean(x, axis=0, keepdims=True)
    var = np.var(x, axis=0, keepdims=True)
    
    x_norm = (x - mu) / np.sqrt(var + eps)
    out = gamma * x_norm + beta
    
    cache = (x, x_norm, mu, var, gamma, beta, eps)
    return out, cache
```
