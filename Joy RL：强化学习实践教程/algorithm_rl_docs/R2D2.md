# R2D2（Recursive Distributional Distributed DQN）

> Recursive, Resourceful, Recurrent, Robust DQN

---

## 1. 算法基础认知

**一句话定义**：R2D2 = LSTM + Distributional + Double + Priority Replay的组合，使用循环网络处理历史。

---

## 2. 核心技术

```python
class R2D2:
    def __init__(self):
        # 1. LSTM核心
        self.network = nn.LSTM(...)
        
        # 2. 分布式Q
        self.z_distribution = Categorical(...)
        
        # 3. Double更新
        self.selection_net = ...
        self.evaluation_net = ...
        
        # 4. 优先级回放
        self.replay_buffer = PrioritizedBuffer(...)
```

---

## 3. 总结

✓ 处理历史信息
✓ 分布式预测
✓ 分布式训练